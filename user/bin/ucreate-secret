if [ -z "$1" ]; then
  notify-send "no site provided"
  exit
fi

password=$(pwgen | head -n 1);
echo $password | xsel -ib;
echo $password;
cd ~/.secrets
echo "$password" | carcosa -Ac "$1/leonid@shagabutdinov.com"

notify-send "secret created: $1"
