#!/usr/bin/env ruby

date = `date --rfc-3339=seconds`.strip().gsub(/[^\w:-]/, '_')
`import -window root -display :0.0 -screen "screen_#{date}.png"`

if $?.exitstatus == 0
	`notify-send "screenshot made"`
else
	`notify-send "failed to make screenshot"`
end
