
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

focus_follows_mouse no

# start a terminal
bindsym $mod+Tab exec emote
# bindsym $alt exec alacritty
bindsym $mod+shift+Return exec alacritty
bindsym $mod+t exec marvex -b alacritty --terminal '@path --title "@title" -e @command' -r 3
bindsym $mod+shift+t exec alacritty
# bindsym $mod+t exec alacritty

# kill focused window
bindsym $mod+c kill

# start dmenu (a program launcher)
bindsym F1 exec rofi -show run -matching fuzzy
bindsym Mod1+space exec rofi -show run -matching fuzzy
bindsym $mod+space exec carcosa-ui '~/.secrets' --mode login-password

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+a focus left
bindsym $mod+s focus down
bindsym $mod+w focus up
bindsym $mod+d focus right

# alternatively, you can use the cursor keys:
# bindsym $mod+Left focus left
# bindsym $mod+Down focus down
# bindsym $mod+Up focus up
# bindsym $mod+Right focus right

# move focused window
# bindsym $mod+shift+j move left
# bindsym $mod+shift+k move down
# bindsym $mod+shift+l move up
# bindsym $mod+shift+colon move right

# alternatively, you can use the cursor keys:
bindsym $mod+shift+a move left
bindsym $mod+shift+s move down
bindsym $mod+shift+w move up
bindsym $mod+shift+d move right

# split in horizontal orientation
bindsym $mod+shift+e split horizontal

# split in vertical orientation
bindsym $mod+e split vertical

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+x layout toggle split
bindsym $mod+shift+x layout tabbed
# bindsym $mod+ctrl+x layout stacking

# toggle tiling / floating
bindsym $mod+shift+z floating toggle

# change focus between tiling / floating windows
bindsym $mod+z focus mode_toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
# bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+q workspace 6
bindsym $mod+6 workspace 7
bindsym $mod+7 workspace 8
bindsym $mod+8 workspace 9
bindsym $mod+9 workspace 10
bindsym $mod+0 workspace 11

# move focused container to workspace
bindsym $mod+shift+1 move container to workspace 1; workspace 1
bindsym $mod+shift+2 move container to workspace 2; workspace 2
bindsym $mod+shift+3 move container to workspace 3; workspace 3
bindsym $mod+shift+4 move container to workspace 4; workspace 4
bindsym $mod+shift+5 move container to workspace 5; workspace 5
bindsym $mod+shift+q move container to workspace 6; workspace 6
bindsym $mod+shift+6 move container to workspace 7; workspace 7
bindsym $mod+shift+7 move container to workspace 8; workspace 8
bindsym $mod+shift+8 move container to workspace 9; workspace 9
bindsym $mod+shift+9 move container to workspace 10; workspace 10
bindsym $mod+shift+0 move container to workspace 11; workspace 11

# move focused container to workspace
bindsym $mod+ctrl+1 move container to workspace 1
bindsym $mod+ctrl+2 move container to workspace 2
bindsym $mod+ctrl+3 move container to workspace 3
bindsym $mod+ctrl+4 move container to workspace 4
bindsym $mod+ctrl+5 move container to workspace 5
bindsym $mod+ctrl+q move container to workspace 6
bindsym $mod+ctrl+6 move container to workspace 7
bindsym $mod+ctrl+7 move container to workspace 8
bindsym $mod+ctrl+8 move container to workspace 9
bindsym $mod+ctrl+9 move container to workspace 10
bindsym $mod+ctrl+0 move container to workspace 11

# reload the configuration file
bindsym $mod+shift+F4 reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# bindsym $mod+shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+semicolon resize shrink width 3 px or 3 ppt
bindsym $mod+apostrophe resize grow width 3 px or 3 ppt

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.

        bindsym j resize shrink width 25 px or 25 ppt
        bindsym k resize grow height 25 px or 25 ppt
        bindsym i resize shrink height 25 px or 25 ppt
        bindsym l resize grow width 25 px or 25 ppt

        bindsym shift+j resize shrink width 5 px
        bindsym shift+k resize grow height 5 px
        bindsym shift+i resize shrink height 5 px
        bindsym shift+l resize grow width 5 px

        bindsym ctrl+j move left 5 px
        bindsym ctrl+k move down 5 px
        bindsym ctrl+i move up 5 px
        bindsym ctrl+l move right 5 px

        # same bindings, but for the arrow keys
        # bindsym Left resize shrink width 2 px or 2 ppt
        # bindsym Down resize grow height 2 px or 2 ppt
        # bindsym Up resize shrink height 2 px or 2 ppt
        # bindsym Right resize grow width 2 px or 2 ppt

        # bindsym shift+Left resize shrink width 20 px or 20 ppt
        # bindsym shift+Down resize grow height 20 px or 20 ppt
        # bindsym shift+Up resize shrink height 20 px or 20 ppt
        # bindsym shift+Right resize grow width 20 px or 20 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#         status_command i3status
# }

# hide_edge_borders both
new_window 1pixel
for_window [class="^.*"] border pixel 1

bindsym $mod+g exec thunar
# bindsym $mod+backslash exec code
bindsym $mod+o exec firefox
bindsym $mod+shift+o exec google-chrome --check-for-update-interval=604800
bindsym $mod+ctrl+shift+o exec google-chrome --user-data-dir=/home/leo/.local/chromium-debug --remote-debugging-port=9222
bindsym $mod+h exec translator
bindsym $mod+shift+h exec translator "$(xsel -op)"

bindsym $mod+comma exec /home/leo/.bin/uvolume -4%
bindsym $mod+period exec /home/leo/.bin/uvolume +4%
bindsym $mod+slash exec volume-toggle
bindsym $mod+ctrl+slash exec mic-toggle
bindsym $mod+shift+slash exec ~/.local/seletskiy/bin/sound-switch
bindsym $mod+shift+Escape exec i3lock -c 000000 && sudo systemctl suspend
bindsym $mod+ctrl+Escape exec i3lock -c 000000
# bindsym $mod+ctrl+u exec uidle
# bindsym $mod+shift+u exec upwork-suspend
bindsym $mod+m exec ushow-status
bindsym $mod+ctrl+shift+Tab exec ~/.bin/switch-display
bindsym $mod+ctrl+shift+backslash exec ~/.bin/toggle-theme
# bindsym $mod+F11 exec ~/.bin/toggle-notifications
bindsym $mod+backslash exec deepin-screenshot
bindsym $mod+shift+backslash exec ~/.bin/vpnup
bindsym $mod+ctrl+backslash exec ~/.bin/vpndown
# bindsym $mod+ctrl+F12 exec ~/.bin/toggle-touchpad
bindsym $mod+shift+comma exec ~/.bin/backlight -d 5
bindsym $mod+shift+period exec ~/.bin/backlight -i 5

bindsym $mod+shift+p exec newpass --modal
bindsym $mod+ctrl+p exec newpass --clipboard

bindsym $mod+F9 exec uscreenshot --active-window
bindsym $mod+shift+F9 exec uscreenshot
bindsym $mod+ctrl+F9 exec uscreenshot --active-window --edit
bindsym $mod+ctrl+shift+F9 exec uscreenshot --edit
bindsym $mod+Home exec uscreencast
bindsym $mod+shift+Home exec uscreencast --active-window

bindsym $mod+ctrl+n exec enter-text "Leonid Shagabutdinov"
bindsym $mod+shift+n exec enter-text "leonid@shagabutdinov.com"
bindsym $mod+ctrl+shift+l exec enter-text "Definition of done:\n\n*\n\nComments:\n\n* "
bindsym $mod+ctrl+shift+q exec enter-text "**question**: "
bindsym $mod+ctrl+shift+i exec enter-text "**issue**: "
bindsym $mod+ctrl+shift+n exec enter-text "**nitpick**: "
bindsym $mod+ctrl+shift+c exec enter-text "**chore**: "
bindsym $mod+ctrl+shift+s exec enter-text "**suggestion**: "
bindsym $mod+ctrl+shift+t exec enter-text "**thought**: "
bindsym $mod+ctrl+shift+w exec enter-text "Welcome to the team!! 👏👏"
bindsym $mod+BackSpace exec utodoist
bindsym $mod+shift+BackSpace exec createtodo-popup --date today
bindsym $mod+shift+equal exec grayscalectl disable
bindsym $mod+ctrl+equal exec grayscalectl enable

bindsym $mod+minus move workspace to output left
bindsym $mod+equal move workspace to output right
bindsym $mod+v exec ~/.bin/connect-bluetooth-devices
bindsym $mod+shift+F8 exec ~/.bin/disconnect-headphones

no_focus [instance="Firefox"]
no_focus [instance="Chrome"]
no_focus [class="Xfce4-notifyd"]
